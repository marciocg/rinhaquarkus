worker_processes auto;

events {
    use epoll;
}

http {
    access_log off;
    upstream api {
        server api01:8080;
        server api02:8080;
        keepalive 250;
    }
    server {
        listen 9999;
        location / {
            proxy_pass http://api;
        }
    }
}